<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>OCR – Extract Text from Images</title>

  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: linear-gradient(135deg, #1d976c, #93f9b9);
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      margin: 0;
      padding: 16px;
      color: #fff;
    }

    .upload-container {
      background: rgba(255, 255, 255, 0.15);
      backdrop-filter: blur(12px);
      padding: 2.5rem;
      border-radius: 18px;
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.35);
      width: 380px;
      max-width: 100%;
      text-align: center;
    }

    .upload-container h1 {
      margin-bottom: 0.5rem;
      font-size: 1.8rem;
      font-weight: bold;
    }

    .upload-container p {
      margin-bottom: 1.5rem;
      font-size: 1rem;
      color: #e6e6e6;
      line-height: 1.5;
    }

    .form-group {
      margin-bottom: 1.5rem;
    }

    input[type="file"] {
      display: block;
      width: 100%;
      padding: 10px;
      border: 2px dashed #fff;
      border-radius: 10px;
      background-color: transparent;
      cursor: pointer;
      color: #fff;
    }

    input[type="file"]::-webkit-file-upload-button {
      background: #fff;
      color: #1d976c;
      border: none;
      padding: 8px 12px;
      border-radius: 6px;
      cursor: pointer;
      font-weight: bold;
      transition: 0.3s;
    }

    input[type="file"]::-webkit-file-upload-button:hover {
      background: #1d976c;
      color: #fff;
    }

    .btn {
      background: #ff7eb3;
      border: none;
      padding: 10px 22px;
      border-radius: 8px;
      color: #fff;
      font-weight: bold;
      cursor: pointer;
      transition: 0.3s;
    }

    .btn:hover {
      background: #ff5277;
      transform: scale(1.05);
    }

    /* Mobile support */
    @media (max-width: 480px) {
      .upload-container {
        padding: 1.8rem;
      }

      .upload-container h1 {
        font-size: 1.5rem;
      }

      .btn {
        width: 100%;
      }
    }
  </style>
</head>

<body>
  <div class="upload-container">
    <h1>OCR Text Extractor</h1>
    <p>Upload one or more images, and our OCR engine will extract the text for you.</p>

    <form id="ocrForm" action="/image" method="POST" enctype="multipart/form-data" novalidate>
      <div class="form-group">
        <input
          type="file"
          id="ocrFiles"
          name="uploaded_file"
          accept="image/*"
          multiple
        >
      </div>

      <input type="submit" value="Extract Text" class="btn">
    </form>
  </div>

  <script>
    const form = document.getElementById('ocrForm');
    const fileInput = document.getElementById('ocrFiles');

    const allowedMimeTypes = [
      'image/jpeg',
      'image/png',
      'image/jpg',
      'image/webp',
      'image/gif',
      'image/bmp',
      'image/tiff'
    ];

    // File validation
    fileInput.addEventListener('change', () => {
      const files = fileInput.files;

      if (!files.length) {
        return;
      }

      for (let file of files) {
        if (!allowedMimeTypes.includes(file.type)) {
          alert('File not supported.');
          fileInput.value = '';
          return;
        }
      }
    });

    // Form validation
    form.addEventListener('submit', (e) => {
      const files = fileInput.files;

      if (!files.length) {
        e.preventDefault();
        alert('Please add file in inputs.');
        return;
      }
    });
  </script>
</body>
</html>
